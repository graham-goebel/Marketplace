/* SheetJS placeholder — run `npm install && node install-deps.js` to enable Excel support */
/* CSV files work without this library. Only .xlsx/.xls require SheetJS. */
if (typeof window !== 'undefined') {
  window.XLSX = window.XLSX || undefined; // signals unavailable to popup.js
}
